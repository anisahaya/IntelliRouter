interface:
  display_name: "Intelligent Model Router"
  short_description: "Route tasks across host-native agents and models"
  default_prompt: "Use $intelligent-model-router to choose locally from the native agents or models available in this host, then execute the task."
