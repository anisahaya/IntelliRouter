interface:
  display_name: "Intelligent Model Router"
  short_description: "Choose the best model across configured providers"
  default_prompt: "Use $intelligent-model-router to compare every available configured model, select the best one for this task, and execute through it."
